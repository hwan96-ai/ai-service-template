# Local AI run artifacts — generated by tools/ai-autopilot.ps1
ai-runs/*
!ai-runs/.gitkeep

# Local Claude Code CLI state (per-machine permission settings, session data)
.claude/*
# Exception: shared agent rules are intentional project content, not CLI state
!.claude/rules/

# Accidental Unix-style home-directory artifacts from local tooling
/~/
